# Correctness Review — round 4

Grounding complete. Ticket VHS-37 retrieved from namespace `skills` (record `07aa6cc6-…`, tag-exact). Spec, brief, `CLAUDE.md`, `AGENTS.md` read fresh from disk. HEAD is `124700b`, matching the spec's stated verification point; `git log -10` on the five touched paths shows `ea5c2b0` (VHS-36, 2026-09-08) as the most recent commit to `skills/spec-cycle/SKILL.md` — the spec's anchors were verified after it, and I re-verified every anchor it names today (Phase 1 `:294`/`:296`, 2b `:365-368`/`:378-383`, 2d `:411-424`, 2e `:426-464` with `:430`/`:431`/`:432`/`:435-441`/`:446-461`, 2f `:470-484` incl. `Reviews at:` `:478` and `What would you like to do?` `:480`, 2f-i step 4 `:542`/step 5 `:555`, 2g `:593-601`, Phase 3 `:626-659`, Failure modes `:685`; agents `correctness:18/38-39/51`, `edge-cases:18/38-39/51`, `conventions:20/43-44/56`, `scalability:18/30/40`, step 6→7 slot in all four; output contracts `138/133/127/76`; `AGENTS.md:101`). Every test-plan baseline re-ran clean today (`Address every P0 and P1 finding`→1, `Deferred (P2+)`→7, `exactly two`→0, `=== FOLLOW-UPS`→0, `Design [0-9]|D1[0-7]`→0, `` `:[0-9]+ ``→0, `(grill)`→0, `same character as the opener`→0, `DEFERRED`→0 in all four agents, `never enters the gate`→0 in both tracked docs, `grep -rn Deferred` on ship-spec/spec-close→0). I also diffed the two extent-clause copies in the spec (§ Design 2 :104 and § Design 7 :182) programmatically: byte-identical, 822 chars, from `runs from its heading to` through `quote fenced text safely.`

The ticket's original "valid + out-of-scope + non-trivial" test is superseded by brief Decision 2 with an explicit rationale and an operator reaffirmation dated 2026-09-08; not a ticket↔brief conflict.

## Closure of round 3 findings

| Lens | ID | Title | Status | Evidence |
|---|---|---|---|---|
| correctness | F-1 | R2(c)'s discharge precondition excludes the rows that can actually be cited | CLOSED | Resolved in the opposite direction, coherently. R2(c) (:127) stays state-based; step 4 (:94) now discharges only when "that row itself records a P0 with `Scope: in-scope` — R2(c)'s precondition"; the block (:182) expects `Discharged:` "only when the row itself records a P0 with `Scope: in-scope`". Consequence 1 (no-remedy P0) cannot fire: the only row the block now files a citation-tracking violation on is P0+in-scope, which is ceiling-class, and step 0 (:86) names the R2(c) discharge as its remedy. Consequence 2 is routed to R5's operator-drops-by-hand path (:130). Consequence 3 (divergent records) is gone — the three invocations and the rule now state one precondition. |
| correctness | F-2 | Line-number self-references in text destined for `SKILL.md` | CLOSED | :138 now reads "the history the 2e 'Do not delete history' bullet protects" with the `:432` citation moved into an "(Authoring note, not shipped text: …)" parenthetical; :129 replaces `(:288-289)` with "the escape Phase 0's re-run pin already names" plus an authoring note; row 8 (:199) adds ``grep -cE '`:[0-9]+'`` → 0, baseline verified 0 today. |
| correctness | F-3 | No round-4 title suffix for a deferral | CLOSED | :100 "…and a deferral with ` — deferred: D-<n>`"; :77 "a rejection, fold, or deferral is recorded as a suffix". |
| correctness | F-4 | R2's edit list stated as exhaustive but R6 renumbers | CLOSED | :127 ends "Rule R6's merge may also renumber a row's `D-<n>` id." |
| correctness | F-5 | Two senses of "discharge" | CLOSED | :77 "R2(c)'s 'discharge' is row-scoped and unrelated to 2f-i step 4's use of the word for a grill decision." |
| edge-cases | F-1 | Grill row malformed by construction | CLOSED | :96 defines the `(grill)` variant (one entry permitted, `§ (operator decision — routing tests not run)`, `Scope: operator — round-4 grill`); :182 exempts it from the two-entry floor and the ceiling test; :167 accepts `operator` as a scope token; :77 records it; rows 6 (:197) and 8 (:199) grep `(grill)`. Walked the full path (option 4 → row → next invocation round 1): no lens files a violation, and an in-scope-P0 grill row is re-filed under the D14 exception, folded at its own sites, row left live — no deadlock. Template residue is my F-2 below (new, P2). |
| edge-cases | F-2 | Step 4 discharges any cited row and folds only the new finding's sites | CLOSED | :94 gates the discharge on the row's own P0+in-scope state, folds "at the union of this finding's sites and the row's `Propagation sites`", and adds the explicit else-branch ("fold the finding at its own sites … leave the row live"); :182 mirrors both halves. |
| edge-cases | F-3 | Fence rule fixes opening but not closing | CLOSED | Pairing rule ("same character as the opener and is at least as long") present in both copies, verified byte-identical (822 chars); rows 6 and 8 grep `same character as the opener`. |
| edge-cases | F-4 | Routing violations exempt from routing but not the recount | CLOSED | :135 head sentence: recount does not run on `routing violation:` titles; a disposition naming only `§ Deferred — follow-up required` is not a trigger; discharge fold sections are triggers and "resolve by the last bullet". |
| edge-cases | F-5 | Row severity can never be raised | CLOSED (deliberate non-application) | Ledgered at :250 with a three-site rationale and the residual visibility path. |
| edge-cases | F-6 | `Discharged:` has no re-anchoring path | CLOSED | R2(b) at :127 now lists "`Where`, `Propagation sites`, or `Discharged:`"; :182 adds "A `Discharged:` entry naming a section that no longer exists is re-anchored under R2(b), not a violation; file a P2 …". |
| conventions | F-1 | Ref-doc / AGENTS clause attaches to the wrong conjunct | CLOSED | :21 now gives line 81 in full as an em-dash aside on the P0/P1 conjunct and pins `AGENTS.md:101` to "the same em-dash aside … before '; P2+ do not.'". |
| conventions | F-2 | Fifth copy of the extent grammar unchecked | CLOSED | Row 6 (:197) extracts the clause from `SKILL.md` and diffs it against `agents/spec-reviewer-correctness.md`; :180 states the byte-identity requirement. Verified the two spec-side copies are already identical. |
| conventions | F-3 | Ref-doc reconciliation omits the round-4 protocol | PARTIAL (P2) | :21 takes the "state the boundary" option, but the boundary sentence's anchors are off by one and one of them names a line the same cell edits — see F-1 below. |
| conventions | F-4 | Round's additions missing from `### Minor additions` | CLOSED | :77 now carries the ` (recurrence of D-<m>)` suffix, the ` [discharged: <value>]` render, and this round's `(grill)` exemption. |
| conventions | F-5 | Unscoped `lint.py --strict` unexplained | CLOSED | :192 "Deliberately unscoped — scoping lint to the five changed paths (VHS-15's form) surfaces `missing-requires` WARNs on the four agent files themselves…". |

## Findings

### F-1: The reference-doc reconciliation boundary names `:88` as left-alone in the same table cell that edits `:88`, and mis-anchors the round-4 protocol by one line
**Severity:** P2
**Pre-ship recommended:** yes
**Where:** spec.md:21 (§ Scope, `docs/spec-workflow-reference.md` / `AGENTS.md` row)
**Claim:** "…line 88's paragraph gains `DEFERRED` in the status set and this sentence, verbatim: 'In every round, including round 1, each reviewer also checks…'. … The reference doc is reconciled only where it restates a sentence this spec edits; its summaries of the round-4 protocol (`:86`) and the two console renders (`:88`, `:90-96`) are left to the skill."
**Why this is wrong:** Both anchors in the closing clause are shifted one line up, and one of them collides with an edit the same cell mandates. Current file (`grep -n`, verified today):
- `docs/spec-workflow-reference.md:85` — "7. **If still red at round 4:** Targeted rewrite — classify each spec section as FROZEN (no unresolved P0/P1) or REWRITE…" — this is the round-4 protocol summary, not `:86`.
- `:86` — "8. **If still red after round 4:** Halt. Present remaining P0/P1 and ask the user what to do…" — this is the halt render, i.e. one of "the two console renders".
- `:88` — "**Round 2+ closure tracking:** Each reviewer reads all prior-round reviewer reports … CLOSED, PARTIAL, REOPENED, or NEW. Reopened items are P0 …" — this is the paragraph the same cell says gains `DEFERRED` and the verbatim `including round 1` sentence, and it is not a console render at all.

So the cell simultaneously instructs the implementer to edit `:88` and lists `:88` among the lines "left to the skill". The intended sentence is plainly "the round-4 protocol (`:85`) and the two console renders (`:86`, `:90-96`)". The descriptive labels make the intent recoverable and the operative edits are specified verbatim and gated by checklist row 10 (`grep -c 'DEFERRED' docs/spec-workflow-reference.md` ≥ 1; `grep -cF 'including round 1'` → 1, baseline 0 today), which is why I grade this P2 rather than P0 — no shipped byte is at risk and the contradiction is caught downstream. But this is the sentence that exists solely to define a reconciliation boundary in response to conventions/R3/F-3, and as written it defines that boundary wrongly in two of its three anchors, so conventions/R3/F-3 is only partially closed.
**Suggested fix:** In :21, replace the closing clause with: "The reference doc is reconciled only where it restates a sentence this spec edits; its summary of the round-4 protocol (`:85`) and the two console renders (`:86`, `:90-96`) are left to the skill." No other change; the three edit instructions (`:81`, `:84`, `:88`) and row 10 are already correct.

### F-2: The `(grill)` row variant landed in the prose sites but not in the two templates that state the constraints it relaxes
**Severity:** P2
**Pre-ship recommended:** yes
**Where:** spec.md:118 and spec.md:113 (§ Design 2, the row-shape block), spec.md:157 (§ Design 5, the render shape) — against spec.md:96, :167, :182
**Claim:** Row shape, `**Propagation sites:** § <a>; § <b> (new); …   (two or more entries; `(new)` marks a section the fix would create; at least one must exist)`; `**Deferred in:** round <n>   (the round the row was appended; for a revert row, the recount round)`. Render shape, `  - <D-n>  <lens>/R<n>/F-<k>  <P0|P1>  <in-scope|out-of-scope>  "<title>"  — Follow-up: <value>`.
**Why this is wrong:** This round's fix for edge-cases/R3/F-1 created a third legal row class and a third legal scope token, and updated every prose site — the producer sentence (:96), the render's extraction rule (:167, which now accepts `operator`), the reviewers' block (:182), the roll-up (:77), and rows 6/8 — but not the two fenced blocks that ship into `SKILL.md` alongside them:
- :118's parenthetical states "two or more entries" as the rule, which :96 and :182 both now exempt a `(grill)` row from, and :113's `Deferred in:` gloss enumerates the two non-grill forms without the `round 4 (grill)` form the reviewers' exemption keys on verbatim.
- :157's scope column enumerates `<in-scope|out-of-scope>`, while the extraction rule three lines below accepts `in-scope`, `out-of-scope`, `operator`, or `?`. A `(grill)` row renders `operator` in a column whose own template says that value cannot occur. (`?` was already unenumerated before this round; `operator` is the first value the mechanism produces routinely.)

Both blocks are inserted verbatim into `SKILL.md`, so the shipped skill carries a template that contradicts the rule printed beside it. Nothing breaks — the adjacent prose governs and an author reading the section in order sees the exemption first — which is why this is P2, not P1. But it is the same one-fix-two-templates propagation miss the ticket exists to route away from, and it is the residue of a P1 fix, so it is the likeliest candidate to be re-filed next round.
**Suggested fix:** Three short edits, all inside blocks this spec already writes. :118 → `…(two or more entries, except a `(grill)` row — see above; `(new)` marks a section the fix would create; at least one must exist)`. :113 → `**Deferred in:** round <n>   (the round the row was appended; `round 4 (grill)` for a Settled grill decision; for a revert row, the recount round)`. :157 → `<in-scope|out-of-scope|operator>`. Optionally extend row 8 with `grep -cF 'in-scope|out-of-scope|operator' skills/spec-cycle/SKILL.md` → 1 so the render placeholder is pinned.

### F-3: R2(c) and step 4 state two different fold extents for the same discharge
**Severity:** P3
**Where:** spec.md:127 (rule R2(c)) vs spec.md:94 (§ Design 1 step 4)
**Claim:** R2(c): "fold the finding at every site on **its** `Propagation sites` list in the same round and append one field, `**Discharged:** folded round <n> — § <a>; § <b>`". Step 4: "fold at the **union of this finding's sites and the row's `Propagation sites`**, append `**Discharged:** …` naming every one of them".
**Why this is wrong:** Not a contradiction in outcome — the union is a superset of the row's list, so step 4's fold satisfies R2(c), and the block's verification ("every section it names carries the fold") passes either way. But R2(c) is the rule an author consults when the discharge is reached from the *row* side (the ceiling-class `routing violation: D-<n>` route named in step 0 at :86), and read alone it prescribes a strictly narrower fold and a strictly narrower `Discharged:` value than step 4 does for the same event. The two sentences ship roughly forty lines apart in `SKILL.md`, and the round-3 P1 that produced them was precisely a fold-extent mismatch — leaving the two statements unequal invites the next round to re-file it.
**Suggested fix:** One clause in R2(c): "…fold at every site on the row's `Propagation sites` list — and, when the discharge is triggered by an in-scope P0 filed under the reviewers' exception, at the union of that list and the finding's own sites (step 4) — in the same round, and append one field naming every section folded: `**Discharged:** folded round <n> — § <a>; § <b>`."

## Summary
P0: 0 | P1: 0 | P2: 2 | P3: 1 | P4: 0

STATUS: GREEN
