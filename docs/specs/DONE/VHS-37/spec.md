# VHS-37 — spec-cycle: fold / defer / reject routing for P0/P1 findings

**Brief:** `docs/specs/TODO/VHS-37.brief.md` · **Plane:** VHS-37 (Backlog) · **Spec-cycle:** run 2, green at round 4 (post-green polish applied) (run 1 halted RED after round 4 and was hand-patched at the 2f halt, option 1)

## Goal

Give `/spec-cycle` a fix-routing step so that a valid P0/P1 finding whose fix would have to land on more than one place in the spec is recorded as a deferred follow-up instead of being folded, and so that the reviewer agents accept a well-formed deferral instead of re-filing the finding — in every round of every invocation. The gate formula, the reviewer output contract, and the existing `## Deferred (P2+)` machinery are untouched. The skill reports the proposed follow-ups on the console at both exits and never files a ticket.

## Scope

Anchors verified against `main` at `124700b` (2026-09-08). Commits `ecdfefe` and `c97d4ad` (2026-09-07) touched the 2f-i region directly below the 2f halt block; the implementer re-verifies `:466-487` and 2f-i step 5 (`:555-557`) at implementation time. Plane VHS-27 is open on the same 2f region; whichever ships second rebases.

| Path | Change |
|---|---|
| `skills/spec-cycle/SKILL.md` Phase 1 opening (`:294-296`) | Two sentences: on a re-run with an existing spec, Phase 1 does not re-author; an incomplete existing spec halts with a pointer to the delete-to-re-author escape (Design 2, rule R4). |
| `skills/spec-cycle/SKILL.md` § 2b closure manifest — example block (`:365-368`) and disposition-phrase prose (`:378-383`) | One new example line; `deferred:` added as a fourth disposition phrase; the recount suffix and the revert line defined in the same prose (Design 4). |
| `skills/spec-cycle/SKILL.md` § 2e, both halves (`:426-464`) | Rounds 1–3 (`:428-432`): "Address every P0 and P1 finding" is replaced by the routing step (Design 1), the section shape and rules R1–R3, R5, R6 (Design 2), and the recount (Design 3). Round 4 (`:434-464`): one passage added after the REWRITE definition in the classification bullet at `:435-441` (D15), and one sentence on round-4 dispositions. |
| `skills/spec-cycle/SKILL.md` § 2f halt block (`:466-487`) and Phase 3 drift-check block (`:626-659`) | Each gains the follow-up report block (Design 5). |
| `skills/spec-cycle/SKILL.md` § Failure modes (`:685-`) | One new bullet (Design 6). |
| `agents/spec-reviewer-correctness.md`, `agents/spec-reviewer-edge-cases.md`, `agents/spec-reviewer-conventions.md`, `agents/spec-reviewer-scalability.md` | Three edits each: a clause on the `closure_manifest` input line (`correctness:18`, `edge-cases:18`, `conventions:20`, `scalability:18`); an unnumbered, ungated **Deferred findings** block inserted between step 6 and step 7; `DEFERRED` added to step 7's status enumeration (`correctness:38-39`, `edge-cases:38-39`, `conventions:43-44`, `scalability:30`) plus one sentence after "REOPENED items are P0" (Design 7). The block is byte-identical across the four files. |
| `docs/spec-workflow-reference.md` (`:81`, `:84`, `:88`) and `AGENTS.md` (`:101`) | Line 81 becomes, verbatim: "P0/P1 block shipping — except that a finding the author routed to the spec's `## Deferred — follow-up required` section, under the scope ceiling, is not re-filed by the reviewers and so never enters the gate; P2+ are advisory." and `AGENTS.md:101` takes the same em-dash aside on its "P0/P1 block shipping" conjunct, before "; P2+ do not.", each on a single line; line 84's "Address every P0 and P1" gains the three dispositions; line 88's paragraph gains `DEFERRED` in the status set and this sentence, verbatim: "In every round, including round 1, each reviewer also checks the spec's `## Deferred — follow-up required` rows for well-formedness and does not re-file a finding whose root a well-formed row already carries." Reconciled because every prior spec-cycle behavior change reconciled the reference doc (VHS-15, -32, -33, -36), and VHS-15 reconciled `AGENTS.md` alongside it. The reference doc is reconciled only where it restates a sentence this spec edits; its summary of the round-4 protocol (`:85`) is a one-line compression of the classification bullet, not a restatement of a sentence this spec replaces, and is left to the skill along with the two console renders (`:86`, `:90-96`). |

**Left alone, byte-identical:** § 2d gate (`:411-424`); `## Deferred (P2+)` and every consumer of it — 2e's P2 bullet (`:431`), 2d (`:424`), 2g (`:593-624`), the 2b failure-mode note (`:371`); the 2e "Do not delete history" bullet (`:432`) and the round-4 closed-issues manifest (`:446-461`); 2f-i (`:489-591`); the reviewer output contracts (`correctness:138-176`, `edge-cases:133-165`, `conventions:127-158`, `scalability:76-118`); `skills/ship-spec/`, `skills/spec-close/`.

The scalability agent is in scope although the brief's Scope table names only the three standing agents: it carries the same `closure_manifest` input and the same "REOPENED items are P0" rule (`:40`), so without the change a deferred scalability finding would be re-filed on every scale-on run.

## Decisions

Numbered as in the brief; each says how the design honors it.

1. **Reviewer-side deferral, not gate arithmetic.** Design 7 adds the accept-a-deferral rule to the agents; Design 1 writes nothing into the gate. `total_p0p1` is still summed from the `STATUS` lines only. The number the operator reads is the number of live findings that are not deliberately deferred: a same-root re-file of a well-formed row is suppressed by the reviewer (D14), so it never enters the count, and an in-scope P0 is never suppressed, so the count cannot hide the one class that blocks shipping. A row that records an in-scope P0 is itself a routing violation (Design 7), so silence cannot hide it either.
2. **Scope is a ceiling.** Design 1 step 3: non-trivial + out-of-scope defers at any severity; non-trivial + in-scope defers at P1; an in-scope P0 is always folded, with every site named and edited. The brief's Decision 3 ("more than one site → defer") makes defer the routing for non-trivial findings under the ceiling, not merely a permission, and the spec keeps that.
3. **Non-trivial = more than one propagation site.** Design 1 step 1 makes the site list the first thing written after the validity test, before any edit; sites the fix would create count, but at least one site must already exist. The list is persisted in the deferral row where the next reviewer can check it.
4. **A re-fold finding forces a recount.** Design 3, bounded by D16. A revert defers the original finding; the new finding is still routed by Design 1 steps 1–3 and is deferred only if the ceiling says so.
5. **A second section, not a rename.** Design 2 adds `## Deferred — follow-up required`; `## Deferred (P2+)` and its consumers are not edited. Headings are matched exactly, never by prefix. 2f option 3 is not a reader of the section; the brief lists it with 2g because its re-author path deletes the spec wholesale (rule R4).
6. **The author classifies scope; the reviewer verifies it.** The `Scope` field of the row is written by the author against the brief; Design 7 has the conventions lens verify it (D17). No scope tag is added to the finding contract.
7. **Follow-up report is console output at both exits.** Design 5 renders one block, from the section, in the 2f halt and in Phase 3. No new file.
8. **No numeric cap.** Nothing in Designs 1–7 counts rows.
9. **Gate-inversion tripwire ships separately.** Not in scope.

**Spec-level decisions (not in the brief):**

### D10 — Reject maps to the existing `not applicable:` disposition

`fixed:` / `reworked:` (fold) and `not applicable:` (reject) already exist in the 2b vocabulary; only `deferred:` is new. Reviewers already REOPEN an unjustified `not applicable`.

### D11 — Row shape is carried in both SKILL.md and the four agents

The seven required field labels, the optional eighth (`Discharged:`, rule R2(c)), and the titled heading appear once in 2e (producer) and once per agent as a single-line checklist (consumer). Agents have no include mechanism and must not read the skill file by an install-specific path. The brief's Scope table lists eight items for the row; the spec folds severity into `Finding:` and adds the title.

### D12 — Ticket-id backfill is an operator hand-edit

Resolves brief Risk 1. `Follow-up:` is written as `unfiled` by the skill and edited to the ticket id by the operator when they file it. The skill never writes a ticket id (brief Out-of-scope 6). Rows survive re-runs because Phase 1 does not re-author an existing spec (Design 2, rule R4), so the render prints whatever the field holds.

### D13 — `D-<n>` is the only key; the `Finding` id is provenance

Rows outlive rounds and invocations, and round numbers restart at 1 on every invocation, so no lens/round/finding id is unique across runs. The manifest keeps 2b's two-part `<lens>/F-<k>` form (round-scoped by construction) and points at the row by `D-<n>`; the row's `Finding:` field records `<lens>/R<n>/F-<k>` for the reader. Reviewers match on `D-<n>` and the row title, never on the finding id. The sole exception to the two-part manifest form is Design 3's revert line, which is round-qualified because it names a finding from an earlier round; reviewers treat a round-qualified line as provenance and verify it against the row, not against the prior round's reports.

### D14 — Same-root suppression is bounded by the ceiling

A reviewer does not re-file a well-formed row's finding or a variant of its root; "same root" means the same spec section in `Where` and the same defect the row's title and `Suggested fix` describe. The one exception is an in-scope P0 candidate, which is always filed with the row cited as context. Rationale: without root matching the reviewers, who regenerate findings from spec text every round, re-file the same defect under new wording; without the P0 exception a broad row could hide an unshippable spec.

### D15 — Routing runs at round 4; the deferral section is FROZEN and governed by rule R2

The brief is silent on round 4. In the round-4 protocol `## Deferred — follow-up required` is FROZEN — copied verbatim, never modified by a REWRITE section — and the classification rule's "unresolved P0/P1" test does not apply to it: its contents are governed by rule R2 in every round, and the edits permitted at round 4 are: appending a row routed this round (step 4 of the routing step in the rounds 1–3 half, including a row recording a Settled grill decision), R2's three row edits — repairing the field a `routing violation: D-<n>` names, re-anchoring, and discharging — inserting a missing preamble, and R6's merge of a duplicate section into the first; none is a FROZEN edit and none needs promotion. A finding routed to defer does not put its target section into REWRITE. The passage lands after the REWRITE definition in the classification bullet at `:435-441`. Round-4 rows are verified by the Deferred-findings block on the next invocation's round 1.

### D16 — The recount's revert is available only in rounds 2–3 of this invocation, for a fold made in this invocation whose sites have not since been re-edited

At round 4 the closed-issues manifest (`:446-461`) makes every CLOSED fix a regression constraint, and a site re-edited by a later fold has no defined "original edit" to remove; in both cases the recount runs but resolves by folding again with the corrected site list. On a re-run, a fold made in a prior invocation leaves no in-context record and is never recounted; the re-run's own rounds 2–3 recount their own folds exactly as any invocation does. No round directory is scanned to detect a fold — fold detection is manifest-and-context only. When a revert does fire, the author reads `docs/specs/TODO/<TICKET-ID>.reviews/round-<m>/<lens>.md` to copy the original finding's `Where` and `Suggested fix` verbatim into the row; if that report is missing or unparseable, the recount resolves by folding again with the corrected site list rather than reverting. A revert that does happen supersedes the reverted finding's closed-issues entry: the round-4 author treats an entry whose `finding_id` matches a row's `Finding:` field as constrained by the row, not by the removed fix — stated in the round-4 routing text because `:446-461` is unchanged. This is a bounded carve-out from brief Decision 4 with the brief's own rationale: the round-4 protocol is the untouched region.

### D17 — Only the conventions lens verifies the row's `Scope` field

The Deferred-findings block is byte-identical in the four agents; one sentence in it says the `Scope` field is verified by the conventions lens and read as opaque by the others. Classifying one's own P0 candidate against the brief (needed for D14's exception) is a different act and every lens does it. Brief-authorization of the author's field is that lens's job today (`AGENTS.md` § Parallel review agents; `docs/spec-workflow-reference.md:77`), and the brief's Decision 6 rationale warns against inviting every lens to file scope opinions.

### Minor additions

Recorded so the drift-check can see them: `D-<n>` numbers continue from the highest existing and are never reused; the skill never garbage-collects a row (the operator is the GC; a filed row prints its ticket id and may be dropped by hand); the render prints `(none)` when the section is absent or empty; a reviewer disputes a listed site only when an unmarked site names a section that does not exist; a routing-violation finding carries the fixed title prefix `routing violation:` so a reader can group them — nothing parses it and the finding's shape is otherwise the standing contract; routing's tests do not run inside 2f-i step 4 (a Settled grill decision is the operator's disposition and is applied as written; one that dispositions a finding as a follow-up is satisfied by appending a row); "halt and narrow the brief" is advisory in rounds 1–3 — the author stops and tells the operator, since 2f option 3 is reachable only after round 4; at round 4 there is no next manifest, so a rejection, fold, or deferral is recorded as a suffix on the finding's title in the 2f halt block; a duplicate section heading is merged into the first (rule R6), mirroring 2g's reconcile-rather-than-append idiom; the section's extent is defined by grammar (heading → next level-2 heading outside blockquotes and fenced code blocks) rather than by position, so a copied heading inside a row or a fenced example cannot end it; a row's `Where` and `Propagation sites` entries may be re-anchored or marked removed when a later round renames or deletes the section they name — the skill's only edit to a persisted row's location fields, and the reason the reviewers' one-existing-site test carries a waiver; a deferred `Suggested fix` is stored `> `-prefixed rather than raw, which is the one place a reviewer's text is transformed on the way into the spec; a row whose severity and scope put it under the ceiling is discharged by folding at every listed site and appending a `Discharged:` field — the only skill edit that resolves a row rather than relocating it, and the reason the reviewers' ceiling test is conditional; a defer whose root already has an undischarged row repairs that row under R2(a) instead of appending a second; a brief with no Scope table or no Out-of-scope list classifies in-scope by default; scope is compared by mapping the spec section to the file region it governs, and the row records that region; a blank line inside a stored `Suggested fix` is a bare `>`, and one blank line may close the blockquote; a row appended for a root a discharged row already carries is suffixed ` (recurrence of D-<m>)` and is not a duplicate of it; the render appends ` [discharged: <value>]` to a discharged row's `Follow-up:` value, which still prints verbatim; a row appended by a Settled grill decision is exempt from the two-entry floor and the ceiling test, since routing never ran on it; R2(c)'s "discharge" is row-scoped and unrelated to 2f-i step 4's use of the word for a grill decision.

## Design

### Design 1 — 2e routing step (SKILL.md § 2e, rounds 1–3)

The bullet "Address every P0 and P1 finding." is replaced by:

> - **Route every P0 and P1 finding** to exactly one disposition — **fold**, **defer**, or **reject** — before editing anything, in this order:
>   0. **Test validity.** If the finding is wrong (misread, stale, already handled) → **reject**: disposition it `not applicable: <reason>` in the next manifest and stop. Otherwise continue. A finding whose title begins `routing violation:` is never deferred: it is folded by the R2 edit the violation calls for — the field repair it names (R2(a)), the R6 merge, the preamble insert, or, for a ceiling-class violation, the R2(c) discharge — whatever its site count or scope; disposition it `fixed: repaired D-<n> (§ Deferred — follow-up required)`, `fixed: merged (§ Deferred — follow-up required)`, `fixed: preamble (§ Deferred — follow-up required)`, or `fixed: discharged D-<n> — edited § <a>; § <b> (§ Deferred — follow-up required)`, naming every section a discharge fold touched so the recount can see them.
>   1. **Name the propagation sites.** List every place in the spec the fix must land or create: the Design sub-section, the contract block it restates, the checklist or test-plan row that gates it, the Decision that records it, the Done-when bullet it maps to. A section the fix would add is a site, written `§ <name> (new)`; at least one listed site must already exist — a fix that lands on no existing section is a single additive edit and folds. Write the list down; it is reused in step 3 and, on defer, in the row.
>   2. **Classify scope** against the brief: *in-scope* when the finding's `Where` and its `Suggested fix` fall inside a row of the brief's Scope table and outside every item of its Out-of-scope list; *out-of-scope* otherwise. Cite the file region and the row or item. Compare by subject: map the spec section the finding lands on to the file region that section governs (this spec's own Scope-table row) and test that region against the brief; a `Suggested fix` that adds behavior no brief Scope row covers is out-of-scope. If the brief has no Scope table or no Out-of-scope list, classify **in-scope** and cite `brief: no Scope table` / `brief: no Out-of-scope list` — the safe default, since in-scope can only force a fold, never permit a deferral.
>   3. **Apply the ceiling:**
>      - one site or none → **fold**, whatever the scope or severity;
>      - two or more sites and out-of-scope → **defer**, whatever the severity;
>      - two or more sites, in-scope, P1 → **defer**;
>      - two or more sites, in-scope, P0 → **fold**: edit every site on the list. An in-scope P0 means the spec is inconsistent or unimplementable and cannot ship as a known issue. If the fold is more than the brief supports, stop and tell the operator; there is no mid-loop scope-down path (2f option 3 is reachable only after round 4).
>   4. **Act.** Fold: edit every listed site and disposition it `fixed: edited § <a>; § <b>` or `reworked: …` in the next manifest, naming every section the edit touched. If the finding cites a row `D-<n>` (the in-scope-P0 exception in the reviewers' block) and that row itself records a P0 with `Scope: in-scope` — R2(c)'s precondition — the fold is also the row's discharge: fold at the union of this finding's sites and the row's `Propagation sites`, append `**Discharged:** folded round <n> — § <a>; § <b>` naming every one of them, and disposition the finding `fixed: discharged D-<n> — edited § <a>; § <b> (§ Deferred — follow-up required)`. If the cited row records anything else, it is not discharged: fold the finding at its own sites, disposition it `fixed: edited § <a>; § <b>`, and leave the row live. Defer: first check `## Deferred — follow-up required` for a row with the same root (same section in `Where`, same defect); if one exists and carries no `Discharged:`, repair that row under R2(a) instead of appending and disposition the finding `deferred: D-<n> (§ Deferred — follow-up required; repaired)`. A row carrying `Discharged:` is not a same-root target — its finding was folded; append a new row and suffix its title ` (recurrence of D-<m>)`. Otherwise add a row (shape below), make no other spec edit for that finding, and disposition it `deferred: D-<n> (§ Deferred — follow-up required)`.
>
> Routing's tests do not run inside 2f-i step 4; a Settled grill decision is applied as written. A Settled decision that dispositions a finding as a follow-up is satisfied by appending a row in the shape below with `Deferred in: round 4 (grill)` — the step 4 append above, which R2 records — so 2f-i step 5's re-render shows it. A row whose `Deferred in:` carries `(grill)` records the operator's disposition, not a routing outcome: `Propagation sites:` lists whatever sites the finding names — one entry is permitted, and `§ (operator decision — routing tests not run)` when it names none — and `Scope:` is written `operator — round-4 grill` when step 2 did not run.

The existing P2 bullet (`:431`) and the "Do not delete history" bullet (`:432`) are unchanged.

**Round 4 (D15).** Routing runs before the FROZEN/REWRITE manifest. After the REWRITE definition in the classification bullet at `:435-441`, add: "`## Deferred — follow-up required` is FROZEN and governed by rule R2: the unresolved-P0/P1 test does not apply to it, and these edits are permitted here without promotion: appending a row routed this round (step 4 of the routing step in the rounds 1–3 half, including a row recording a Settled grill decision), R2's three row edits — repairing the field a `routing violation: D-<n>` names, re-anchoring, and discharging — inserting a missing preamble, and R6's merge of a duplicate section into the first. A finding routed to defer does not put its target section into REWRITE. When building the closed-issues manifest, an entry whose `finding_id` matches the `Finding:` field of a row in this section is superseded: its regression constraint is the row, not the removed fix." Design 3's revert is not available at round 4 (D16). There is no next manifest at round 4, so a rejection is recorded by suffixing the finding's title in the 2f halt block with ` — rejected: <reason>`, a fold with ` — folded § <section>`, and a deferral with ` — deferred: D-<n>`.

### Design 2 — the `## Deferred — follow-up required` section

Defined in the same 2e insertion, immediately after the routing step. It is a spec section at the end of the spec, found by exact heading; it runs from its heading to the next level-2 heading (exactly two `#`) that is not inside a blockquote or a fenced code block, or to end of file; a `###`-or-deeper heading does not end the section. A line prefixed `> `, or inside a fenced code block, is never a heading, never a section terminator, and never a row. A fence marker counts only when three or more backticks or tildes begin the line (after at most three spaces) and the line is not prefixed `> `, matched from the top of the file: a marker opens a fence only when no fence is open, and closes one only when it uses the same character as the opener and is at least as long — a shorter or different marker inside an open fence is content, and a marker inside a blockquote neither opens nor closes a fence, so a stored `Suggested fix` may quote fenced text safely. Under the heading, before the first row, a fixed preamble line is always present; the reviewers check for its sentence every round (the Deferred-findings block):

```markdown
## Deferred — follow-up required

> Known, unfixed P0/P1 findings. Not part of this spec's implementation: `/ship-spec` must not implement anything in this section. Each row becomes a follow-up ticket filed by the operator.

### D-1: <title copied from the finding>
**Finding:** <lens>/R<n>/F-<k> (P0 | P1)
**Deferred in:** round <n>   (the round the row was appended; `round 4 (grill)` for a Settled grill decision; for a revert row, the recount round)
**Where:** spec § <section>, <anchor as the reviewer gave it>
**Suggested fix:** (verbatim, as a blockquote — every line of the reviewer's text, blank lines included, prefixed `> `; a blank line inside the text is stored as a bare `>`; one blank line closing the blockquote before `**Propagation sites:**` is permitted; otherwise unedited)
> <the reviewer's Suggested fix>

**Propagation sites:** § <a>; § <b> (new); …   (two or more entries, except a `(grill)` row — see above; `(new)` marks a section the fix would create; at least one must exist)
**Scope:** in-scope | out-of-scope — <file region this spec's Scope table maps the section to>; <brief Scope-table row or Out-of-scope item cited>
**Follow-up:** unfiled
**Discharged:** folded round <n> — § <a>; § <b>   (optional eighth field; present only after rule R2(c))
```

Rules R1, R2, R3, R5, R6 are written into SKILL.md with the shape; R4's normative sentences land in Phase 1 (`:294-296`).

- **R1.** `D-<n>` continues from the highest number already in the section and is never reused. Concurrent `/spec-cycle` invocations over one spec are outside the supported flow — allocation is read-modify-write, last-writer-wins over the whole file, matching 2f-i's posture for `grill.md`.
- **R2.** `Follow-up:` is written as `unfiled` by the skill and is free text thereafter; the operator replaces it with the ticket id when they file one, by hand. `/spec-cycle` never deletes a row and never overwrites `Follow-up:`. It reads rows: the follow-up report at both exits, and every reviewer in every round. The skill appends a new row when routing defers a finding (step 4 of the routing step) or when a Settled grill decision dispositions one as a follow-up. The only edits the skill makes to an existing row are (a) repairing the field a `routing violation: D-<n>` finding names (or the undischarged row a same-root re-file identifies, step 4 of the routing step), or inserting a missing preamble; (b) re-anchoring an entry in `Where`, `Propagation sites`, or `Discharged:` when a later round renames, merges, or removes the section it names: the entry becomes `§ <new heading> (re-anchored round <n>)` or, when nothing replaces the section, `§ <old heading> (removed round <n>)` — the marker goes on that entry, not on the field. Marked entries still count toward the two-entry floor, and a row carrying any marker is exempt from the at-least-one-existing-site test; and (c) **discharging** a row whose `Finding` severity is `P0` and whose `Scope` is, or becomes under an (a) repair, `in-scope` — whether the reviewers surfaced it as a ceiling-class `routing violation: D-<n>` or as the underlying in-scope P0 citing the row: fold the finding at every site on its `Propagation sites` list in the same round and append one field, `**Discharged:** folded round <n> — § <a>; § <b>`; the row is still not deleted, and the follow-up report prints the field after `Follow-up:`. If every `Propagation sites` entry carries a `(removed round <n>)` marker, redo step 1 of the routing step for the row's finding against the current spec, append the new sites to `Propagation sites` (a (b)-class edit), and discharge against those. Rule R6's merge may also renumber a row's `D-<n>` id. The finding record — `Finding`, `Deferred in`, `Suggested fix` — is never rewritten.
- **R3.** A row is not a `## Deferred (P2+)` entry and is never a 2g candidate; 2g reads only `## Deferred (P2+)`, by exact heading.
- **R4.** On a re-run with an existing `docs/specs/TODO/<TICKET-ID>.spec.md`, Phase 1 does not re-author: the existing file is v1 and Phase 2 starts from it, so this section and its `Follow-up:` values survive verbatim. If the existing file lacks any of `## Goal`, `## Scope`, `## Design`, `## Test plan`, `## Test command`, `## Done when`, `## Out of scope`, halt: `existing spec is incomplete; delete docs/specs/TODO/<TICKET-ID>.spec.md to force a clean re-author, or patch it`. To re-author from a changed brief (for example after 2f option 3), delete the spec first — the escape Phase 0's re-run pin already names. Deleting the spec discards `## Deferred — follow-up required` with it; the follow-up report rendered above the 2f menu is the operator's copy — file or copy the rows before deleting. (Authoring note, not shipped text: two sentences at `:294-296`; the re-run pin is at `:288-289`; mirrors 2g's "if the section already exists" idiom at `:600-601`.)
- **R5.** Author-facing: if a later fold happens to resolve a deferred finding's root, the row stays (a discharged row too); a fold that resolves an in-scope-P0 row's root is completed by the R2(c) discharge in the same round; the reviewers' existing rule records the finding CLOSED with the fold as evidence, and the operator may drop the row by hand.
- **R6.** If more than one `## Deferred — follow-up required` heading is present, the first is authoritative: the author merges the later sections into it in the same round, renumbering colliding `D-<n>` ids from the highest in the merged section (the finding records are otherwise unchanged). A duplicate still present when the reviewers run is a routing violation. Renumbered rows keep their finding records; when the author builds the next manifest, a line citing a renumbered row is written with the row's new `D-<n>`, checked against the row's `Finding:` field — the reviewer is never shown a stale id as the key. A prior-round finding title embedding the old id is still copied verbatim per 2b; the line reads `… — fixed: repaired D-7 (renumbered from D-3 by the R6 merge) (§ Deferred — follow-up required)`.

### Design 3 — re-fold recount (SKILL.md § 2e, same insertion)

The recount does not run on a finding whose title begins `routing violation:`, and a disposition whose only named section is `§ Deferred — follow-up required` is not a recount trigger — a row repair is not a fold. A `fixed: discharged` disposition's fold sections are triggers; a later finding on one of them recounts and resolves by the last bullet, since a discharged finding is an in-scope P0. Before routing any other new P0/P1 finding, check its `Where` against every section named on any `fixed:` / `reworked:` line of the manifests built earlier in **this invocation** (a fold made in a prior invocation is not detectable and is not recounted). If it lands on such a section, the new finding is evidence that the original site list was short:

- Redo step 1 of the routing step on the original finding, counting the new finding's site.
- True count now two or more, and the original finding is not an in-scope P0, and this is round 2 or 3, and none of the original fold's sites has been re-edited by a later fold → **revert the original fold** (remove the edit from every site it touched), defer the original finding as a row, and route the new finding through routing steps 1–3 as normal. If the ceiling routes the new finding to **fold** (in-scope P0, or one site or none), the original is still reverted and deferred and the new finding is folded at every site on its own list; only a new finding the ceiling routes to defer becomes the second row. The reverted finding's row copies `Where` and `Suggested fix` verbatim from `docs/specs/TODO/<TICKET-ID>.reviews/round-<m>/<lens>.md`; if that report is missing or unparseable, the next bullet applies. This is the one sanctioned removal of a prior round's edit: the history the 2e "Do not delete history" bullet protects is preserved by the deferral row, which carries the finding record verbatim, plus the round-qualified manifest line. (Authoring note, not shipped text: this reconciles the recount with that bullet at `:432`, which stays byte-identical.) The revert supersedes the reverted finding's closed-issues entry: at round 4 that entry's constraint is its deferral row, not the removed fix. The manifest gets one extra, round-qualified line for the reverted finding: `<lens>/R<m>/F-<k> (P<sev>) "<title>" — deferred: D-<n> (§ Deferred — follow-up required; revert of round <m> fold)`.
- Otherwise (count still one; an in-scope P0; round 4; a site re-edited since; or the round-<m> report unavailable) → patch once more with the corrected site list and append `(recount: <k> sites, fold kept)` to the disposition phrase so the reviewer can see the rule ran.

### Design 4 — manifest vocabulary (SKILL.md § 2b)

Two touches. In the disposition-phrase prose at `:378-383`: add `deferred: D-<n> (§ Deferred — follow-up required)` as a fourth phrase alongside `fixed:`, `reworked:`, `not applicable:` (it carries the § anchor 2b requires); state that a routing-violation fix is dispositioned `fixed: repaired D-<n>`, `fixed: merged`, `fixed: preamble`, or `fixed: discharged D-<n> — edited § <a>; § <b>`, each with the `(§ Deferred — follow-up required)` anchor; state that a `fixed:` or `reworked:` phrase names every spec section the edit touched, not only the Decision that records it; state that a `fixed:` / `reworked:` phrase may carry a `(recount: <k> sites, fold kept)` suffix; and state that a fold reverted under the recount adds one extra, round-qualified line for the original finding even though it predates round N−1, marked `revert of round <m> fold`. In the example block at `:365-368`, add one line:

```text
  - edge-cases/F-4 (P1) "jq empty-array path" — deferred: D-2 (§ Deferred — follow-up required)
```

The "P0/P1 findings only" rule at `:370-371` is unchanged. The "Map each P0/P1 finding to exactly one line" sentence at `:378-379` gains the revert line as its one stated exception, defined in the same prose.

### Design 5 — follow-up report at both exits (SKILL.md § 2f, Phase 3)

The report is rendered from `## Deferred — follow-up required` and nothing else. Its shape, inserted verbatim at both exits:

```text
=== FOLLOW-UPS (proposed; not filed by this skill) ===
  - <D-n>  <lens>/R<n>/F-<k>  <P0|P1>  <in-scope|out-of-scope|operator>  "<title>"  — Follow-up: <value>
```

The extraction rule and one example are written once, as prose directly below the 2f halt block; the Phase 3 copy of the shape is accompanied, below the closing fence, by the sentence "rendered per the extraction rule in § 2f". The example:

```text
  - D-1  correctness/R2/F-3  P1  out-of-scope  "<title>"  — Follow-up: unfiled
  - D-2  edge-cases/R3/F-4   P1  in-scope      "<title>"  — Follow-up: VHS-99
```

Extraction rule: the section's extent is as 2e defines it — no re-parse; the render treats every `###`-or-deeper heading in that span that is not prefixed `> ` and not inside a fenced code block as a row. A heading whose `D-<n>` id does not parse prints as `  - D-?  (malformed row — see § Deferred — follow-up required)`. Severity is the parenthetical in `Finding:`, scope is the token before the **first** em dash in `Scope:` (if that token is none of `in-scope`, `out-of-scope`, `operator`, print `?`), `Follow-up:` is printed verbatim; a row carrying `Discharged:` additionally prints ` [discharged: <value>]` after it and needs no ticket; a missing or unparseable field prints as `?` in its column. The render never omits a heading it found and never edits a row. When the section is absent or has no rows, the block is its header line plus `  (none)`.

- **Red path:** the shape is inserted into the 2f halt block after the `Reviews at:` line and before `What would you like to do?`; the extraction rule and the example follow the halt block as prose. 2f-i step 5 re-renders "the 2f halt block again", so the report reappears there without a separate rule.
- **Green path:** the shape is inserted into the Phase 3 block after the out-of-scope fences (and the scale block when it renders) and before `=== NEXT ===`; the one-line pointer "rendered per the extraction rule in § 2f" is prose below the closing fence, alongside the existing Scale-declaration note — never inside the rendered output.

### Design 6 — failure-mode bullet (SKILL.md § Failure modes)

> - **Deferral row never re-verified downstream.** A row in `## Deferred — follow-up required` is checked for well-formedness, and the section for its preamble, by every reviewer in every round (the ungated Deferred-findings block) and carried unchanged thereafter. The preamble tells `/ship-spec` not to implement it, and neither `/ship-spec` nor `/spec-close` reads the section, so an `unfiled` row the operator never files is a silently dropped finding. The follow-up report at both exits is the only reminder. A spec that carried rows in a prior round and renders `(none)` now is the signature of a truncated write or of an unbalanced fence earlier in the spec, not of an empty section — check fence parity and `git diff` on the spec before proceeding. The section is inert unless the four reviewer agents carry the Deferred-findings block: if a spec shows rows and the reviewers still re-file them, the installed agents are out of date — run `python sync.py install`.

### Design 7 — reviewer agents (four files, three edits each)

1. **Input line.** The `closure_manifest` bullet gains: `… disposition is one of fixed / reworked / not applicable / deferred, optionally suffixed (recount: …); a round-qualified revert line names an earlier-round finding — see the Deferred-findings block`.

2. **Deferred-findings block**, unnumbered and ungated, inserted between step 6 and step 7, byte-identical in all four files (the implementer may lay it out as a lead sentence plus one bullet per rule, provided the four copies stay byte-identical and every literal row 6 greps stays on one line); its extent clause is also byte-identical, from `runs from` onward, with the one in 2e (Design 2), and row 6 diffs the five copies:

> **Deferred findings (every round, including round 1).** Read the spec's `## Deferred — follow-up required` section if present. It runs from its heading to the next level-2 heading (exactly two `#`) that is not inside a blockquote or a fenced code block, or to end of file; a `###`-or-deeper heading does not end the section. A line prefixed `> `, or inside a fenced code block, is never a heading, never a section terminator, and never a row. A fence marker counts only when three or more backticks or tildes begin the line (after at most three spaces) and the line is not prefixed `> `, matched from the top of the file: a marker opens a fence only when no fence is open, and closes one only when it uses the same character as the opener and is at least as long — a shorter or different marker inside an open fence is content, and a marker inside a blockquote neither opens nor closes a fence, so a stored `Suggested fix` may quote fenced text safely. The first non-blank content under the heading must be a blockquote whose text contains the sentence `must not implement anything in this section` (the preamble names `/ship-spec` as the actor; the surrounding wording and any code spans are free); if there is no such blockquote or it lacks that sentence, file a P0 titled `routing violation: missing preamble`. If the heading appears more than once, file a P0 titled `routing violation: duplicate section`. A row is **well-formed** when its `### D-<n>:` heading carries a title and all seven fields are present (Finding, Deferred in, Where, Suggested fix, Propagation sites, Scope, Follow-up; an eighth, Discharged, is optional), every non-blank line between its `**Suggested fix:**` label and its `**Propagation sites:**` label begins with `>` and at most one blank line — the blockquote terminator — sits immediately before the `**Propagation sites:**` label, its `Propagation sites` lists two or more entries, at least one names a section that exists in the spec (waived when any entry carries a `(re-anchored round <n>)` or `(removed round <n>)` marker), and every other entry names an existing section, is marked `(new)`, or carries one of those markers; a re-anchored entry is checked against the new heading it names. A row whose `Deferred in:` field carries `(grill)` is exempt from the two-entry `Propagation sites` floor, from the at-least-one-existing-site and every-other-entry tests on `Propagation sites`, and from the ceiling test; it records an operator decision and is checked only for the presence of its fields. The `Scope` field is verified by the conventions lens against the brief's Scope table and Out-of-scope list by checking the file region the row states against the brief — the lens does not re-derive the mapping, and only a stated region that is itself wrong is a routing violation; a citation of `brief: no Scope table` or `brief: no Out-of-scope list` is verified only for the absence it claims; the other lenses read it as opaque. A row whose `Finding` severity is `P0` and whose `Scope` is `in-scope`, and that carries no `Discharged:` field, is itself a routing violation whatever else it satisfies — the ceiling forbids deferring an in-scope P0; file it once, as a P0 titled `routing violation: D-<n>`, naming the ceiling, unless you are filing the underlying P0 under the exception below, which cites the row and names the ceiling instead. A `Discharged:` field is verified like a `fixed:` disposition: every section it names carries the fold. A row cited when an in-scope P0 was filed under the exception must carry `Discharged:` by the next round only when the row itself records a P0 with `Scope: in-scope`; if that P0 was folded and such a row still carries none, that is the `routing violation: D-<n>` this block files. A cited P1 or out-of-scope row stays live and is not expected to carry the field. A `Discharged:` entry naming a section that no longer exists is re-anchored under R2(b), not a violation; file a P2 naming the entry if it carries no marker. A row titled `(recurrence of D-<m>)` is a new row, not a duplicate of the discharged one. Every lens applies this test: it reads the row's own labels and needs no independent scope call. That is different from classifying your own candidate: every lens decides for itself whether a P0 it is about to file is in-scope. Do not file a P0/P1 whose root is a well-formed row — same spec section in `Where`, same defect the row's title and `Suggested fix` describe — **unless the candidate is an in-scope P0**, which is always filed with the row cited as context. A row that is not well-formed, or whose `Scope` is wrong in a way that changes the routing (an in-scope P0 recorded as out-of-scope), is a routing violation: file it once, as a P0 titled `routing violation: D-<n>`, naming the failing field or rule. A `Scope` error that does not change the routing is a P2 correction. A manifest line whose finding id is round-qualified (`<lens>/R<m>/F-<k>`) names an earlier-round finding whose fold was reverted; verify it against the row it cites, not against the prior round's reports, and do not treat its absence from round N−1 as a defect. A disposition marked `renumbered from D-<m>` names the row by its new id; do not REOPEN on the old id in the copied title. Whether a listed site was truly necessary is the author's call; dispute a site only when an unmarked site names a section that does not exist.

3. **Step 7.** The status enumeration becomes `CLOSED, PARTIAL, REOPENED, DEFERRED, or NEW`, and one sentence follows "REOPENED items are P0 …": "A `deferred: D-<n>` disposition is satisfied when row `D-<n>` is well-formed per the Deferred-findings block; mark the finding `DEFERRED`. If no row `D-<n>` exists, mark the finding REOPENED and cite the absent row as the evidence. If the row exists but is not well-formed, mark it REOPENED with the `routing violation: D-<n>` finding this block already filed as its evidence — the defect is reported once, not as a second finding." The illustrative closure-table example is not required to gain a row.

The scalability agent's step 7 has the same enumeration (`:30`) and the same "REOPENED items are P0" anchor (`:40`) and takes the same three edits.

## Test plan

Prose-only change; no code ships. Review checklist for the implementer and the PR reviewer. Run the greps from Git Bash: several literals carry an em dash, and PowerShell can mis-encode them (recorded gate-script pitfall). Assert hunk regions, not counts; shift pre-edit line numbers past the 2e insertion when checking 2f, 2g, and Phase 3; re-verify every backtick phrase with `grep -cF` after the edit. Where a row greps a short token rather than a phrase, that is deliberate: the file is hard-wrapped in places and a phrase can straddle a line break. Every literal a checklist row greps must be written on a single line in the target file — do not wrap a line that carries one; if the surrounding prose is hard-wrapped, the literal starts a new line.

1. `python lint.py --strict` exits 0 with zero ERROR (the pre-existing `missing-requires` WARNs on unrelated skills are not gated). Deliberately unscoped — scoping lint to the five changed paths (VHS-15's form) surfaces `missing-requires` WARNs on the four agent files themselves, which are pre-existing and not this ticket's.
2. `python sync.py install` then `python sync.py status` shows no `differ` row for `skills/spec-cycle/SKILL.md` or the four `agents/spec-reviewer-*.md`; `python sync.py push --dry-run` (flag after the subcommand) reports nothing pending for those five paths, and `git status --porcelain` on them is empty. Pre-existing `dst-only` vendor rows are not this ticket's.
3. `grep -cF 'Address every P0 and P1 finding' skills/spec-cycle/SKILL.md` → 0. `grep -cF '## Deferred — follow-up required' skills/spec-cycle/SKILL.md` ≥ 4 (2e step 4's defer instruction, the shape heading, the D15 passage, the Failure-modes bullet).
4. `git diff -U0 skills/spec-cycle/SKILL.md | grep '^@@'` hunks fall only in: Phase 1 opening (`:294-296`), 2b example block (`:365-368`), 2b disposition prose (`:378-383`), 2e (`:426-464`), 2f halt block, Phase 3 block, Failure modes. No hunk covers `:371`, `:411-424` (2d), `:431` (the P2 bullet), `:489-591` (2f-i), or `:593-624` (2g). This is the assertion that the `## Deferred (P2+)` machinery is untouched.
5. `grep -c 'Deferred (P2+)' skills/spec-cycle/SKILL.md` → 8 or 9 after (7 before): rule R3 is the only new text carrying the literal, twice, and may occupy one or two lines depending on wrap. Row 4 is the real protection; this row only confirms no pre-existing line vanished.
6. `grep -cF 'Deferred findings (every round' agents/spec-reviewer-*.md` → 1 per file, four files; the four blocks are byte-identical (extract each with `sed -n` and `diff`), and the extent clause — from `runs from its heading to` through `quote fenced text safely.` — extracted from `skills/spec-cycle/SKILL.md` diffs clean against the same span in `agents/spec-reviewer-correctness.md` (five copies, one grammar). Per file, each of these is ≥ 1: `grep -c 'DEFERRED'` (0 today), `grep -cF 'must not implement anything in this section'`, `grep -cF 'routing violation: missing preamble'`, `grep -cF 'in-scope P0'`, `grep -cF 'forbids deferring an in-scope P0'`, `grep -cF 'fenced code block'`, `grep -cF 'exactly two'`, `grep -cF 'Discharged:'`, `grep -cF 'begin the line'`, `grep -cF 'non-blank line between'`, `grep -cF '(grill)'`, `grep -cF 'same character as the opener'`.
7. `grep -rn 'Deferred' skills/spec-close/SKILL.md skills/ship-spec/SKILL.md` → 0 hits, unchanged.
8. `grep -cF '=== FOLLOW-UPS' skills/spec-cycle/SKILL.md` → 2 (the shape, at both exits). `grep -cF 'malformed row' skills/spec-cycle/SKILL.md` → 1 (the extraction rule, once under 2f). `grep -cF 'exactly two' skills/spec-cycle/SKILL.md` ≥ 1 (the extent clause in 2e). `grep -cF 'Discharged:' skills/spec-cycle/SKILL.md` ≥ 2 (rule R2(c), the row shape). `grep -cF 'begin the line' skills/spec-cycle/SKILL.md` ≥ 1. `grep -cF 'rendered per the extraction rule' skills/spec-cycle/SKILL.md` → 1 (prose below the Phase 3 fence). `grep -cE 'Design [0-9]|D1[0-7]' skills/spec-cycle/SKILL.md` → 0 and ``grep -cE '`:[0-9]+' skills/spec-cycle/SKILL.md`` → 0 (no spec-internal label or line-number anchor leaks into shipped text; both 0 today). `grep -cF '(grill)' skills/spec-cycle/SKILL.md` ≥ 1 and `grep -cF 'same character as the opener' skills/spec-cycle/SKILL.md` ≥ 1. `grep -cF 'must not implement anything in this section' skills/spec-cycle/SKILL.md` ≥ 1. `grep -cF '**Follow-up:** unfiled' skills/spec-cycle/SKILL.md` → 1 (row shape) and `grep -cF '— Follow-up: unfiled' skills/spec-cycle/SKILL.md` → 1 (the example lives once, under 2f).
9. `grep -cF 'deferred: D-<n> (§ Deferred — follow-up required)' skills/spec-cycle/SKILL.md` ≥ 2 (2b prose, 2e step 4). `grep -cF 'governed by rule R2' skills/spec-cycle/SKILL.md` ≥ 1 (the D15 passage in the round-4 half). `grep -cF 'fixed: discharged' skills/spec-cycle/SKILL.md` ≥ 2 (2b prose, 2e step 0).
10. `grep -cF 'Address every P0 and P1.' docs/spec-workflow-reference.md` → 0; `grep -c 'DEFERRED' docs/spec-workflow-reference.md` ≥ 1; `grep -cF 'including round 1' docs/spec-workflow-reference.md` → 1 (the sentence quoted in the Scope table). `grep -cF 'never enters the gate' docs/spec-workflow-reference.md` → 1 and `grep -cF 'never enters the gate' AGENTS.md` → 1.

## Test command

```
python lint.py --strict
```

Checklist rows 2–10 are run by hand by the implementer and pasted into the PR body.

## Done when

Mapped 1:1 to the brief's Done-when:

- **`/spec-cycle` 2e names the fold / defer-to-ticket / reject dispositions and the test that forces defer-to-ticket.** — Design 1 (dispositions and the four-step test), Design 3 (recount). Checklist rows 3, 4, 9.
- **Deferred entries that become tickets carry the ticket id.** — Design 2's `Follow-up:` field with D12's backfill rule and rule R4's re-run preservation; Design 5 renders the field's value verbatim. Checklist rows 4 (Phase 1 hunk), 8.
- **`lint.py --strict` reports zero ERROR; `sync.py status` clean; `sync.py push` round-trips byte-for-byte.** — Checklist rows 1, 2; the Test command.

## Out of scope

Carried from the brief, unchanged:

1. The gate-inversion tripwire and its "defers more than it folds" variant (Decision 9).
2. Any change to the reviewer output contract, including an `In-fence` tag (Decision 6).
3. A `<TICKET-ID>.follow-ups.md` artifact, or any `/ship-spec` / `/spec-close` consumption of the deferral sections (Decision 7). The preamble in Design 2 is a fence inside the spec, not a downstream consumer.
4. Renaming or re-scoping `## Deferred (P2+)`, or changing 2g (its only reader) or 2f option 3 (Decision 5).
5. Any numeric cap on deferrals (Decision 8).
6. `/spec-cycle` filing, transitioning, or commenting on a ticket (D12: the operator backfills the id by hand).
7. Any change to the 2d gate formula (Decision 1).

Added by this spec:

8. A mid-loop scope-down path (a halt before round 4). The brief inherits the same gap; Design 1 step 3 makes the advice advisory rather than inventing the mechanism.
9. Atomic (tmp-rename) writes for the spec file in Phase 1 and 2e. R4's incompleteness halt covers an interrupted write that lands before `## Out of scope`; a truncation after it can silently drop `## Deferred — follow-up required` and is not detected (Design 6 names the signature). The write discipline is a separate change.
10. A size bound on a row. `Suggested fix` is stored verbatim however long it is; provenance is worth the bytes, and a row is read, never re-authored.

## Deferred (P2+)

- **run-1 correctness/R1/F-7** (ceiling as mandate) — not applied: the brief's Decision 3 is itself the mandate ("more than one site named → non-trivial → defer"); Decision 2's restatement now says so.
- **run-1 edge-cases/R1/F-9** (heading prefix-confusable with `## Deferred (P2+)`) — not applied: the brief names the section in Decision 5 and its Scope table; Decision 5 and rule R3 now say headings are matched exactly. Revisit if a 2g misfile is ever observed.
- **run-1 conventions/R1/F-6** (closure-table example row) — partially applied: the enumeration gains `DEFERRED`; the illustrative example table is not required to change.
- **run-1 edge-cases/R2/F-9(a)** (rows accumulate; filed rows never removed) — not applied: brief Decision 8 fences caps; the render prints the ticket id for a filed row and the operator drops it by hand (minor-additions bullet). The version-skew half, F-9(b), is applied in Design 6.
- **run-1 edge-cases/R2/F-10** (render suffix for a row a later fold resolved) — not applied: the skill does not judge whether a row is resolved (rule R5 leaves that to the reviewers' closure table and the operator). The severity/scope sub-parse half is applied in Design 5.
- **run-1 edge-cases/R2/F-1** (one routing violation filed by N lenses counts N) — not special-cased: any finding filed by several lenses counts once per lens today; the block says "file it once" per lens, and 2b's author-side merge is the existing dedupe.
- **run-1 edge-cases/R2/F-4 residue** (a two-entry row of one existing site plus one `(new)` site is not disputable) — accepted: the one-existing-site floor restores falsifiability for the all-new case; whether a `(new)` site is truly needed is the author's call by design (brief Decision 3 makes the site list the author's discipline, checked after the fact).
- **run-1 edge-cases/R4/F-5** (`Where` never checked for resolution; a renamed section defeats D14's root match) — not applied: rule R2(b) already lets the author re-anchor `Where`; making it mandatory and adding a resolution check would land on R2, the Design 7 block, and D14, which is the multi-site fold this ticket routes away from. Revisit if a renamed section is observed to re-file a deferred finding.
- **run-1 correctness/R4/F-5** (Phase 1 anchor `:294-296` excludes the insertion point) — not applied: "Phase 1 opening" is the region label row 4 asserts; the two sentences land after the `Output path:` line at `:296`, and the Scope preamble already makes the implementer re-verify anchors at implementation time.
- **run-2 edge-cases/R1/F-12(b)** (a later schema change makes every old row a violation) — not applied: the field list is versioned with the agents; a future field is that spec's job to make optional, as `Discharged` is made optional here.
- **run-2 edge-cases/R1/F-8** (option 3's delete discards rows) — partially applied: R4 now warns and names the 2f render as the operator's copy; a confirmation prompt is not added, since it would change the 2f menu (Out-of-scope 4).
- **run-2 edge-cases/R3/F-5** (a row's recorded severity can never be raised) — not applied: the fix lands on R2, the follow-up report's severity parse, and the block's ceiling test — three sites, the multi-site class this ticket defers; the higher-severity candidate remains visible in the round report that filed it.
- **run-2 conventions/R1/F-5** (`### D<n>` subsections) — applied structurally: D10–D17 and the minor-additions roll-up are `###` subsections; the brief-carried 1–9 stay a numbered list because the drift-check maps them by the brief's numbering.
- **run-2 edge-cases/R4/F-1 (roll-up half)** and **run-2 conventions/R4/F-2** (round-4 additions absent from `### Minor additions`) — not applied in polish: 2g may not edit the Decisions section; the block and step 4 carry the operative text, and the roll-up is drift-check visibility only.
- **run-2 correctness/R4/F-3** (R2(c) and step 4 state different fold extents) — not applied: reached from the row side (a ceiling-class violation) there is no citing finding, so the row's own list is the whole fold; step 4 states the union for the citing case, and the union is a superset, so both verify.
- **run-2 edge-cases/R4/F-2** (the `(grill)` exemption survives an R2(a) repair that makes the row P0 + in-scope) — not applied: bounding the exemption to `Scope: operator` lands on the block, step 4, and the roll-up; D14's exception still files and folds any in-scope P0 candidate on that root.
- **run-2 edge-cases/R4/F-3** (a live row whose in-scope slice was folded carries no marker) — not applied: a `Partly folded:` field lands on R2, step 4, the render, the block, and R5 — the multi-site class this ticket defers; the operator scopes the follow-up against the current spec.
- **run-2 edge-cases/R4/F-4** (a Settled grill decision with several `ref:` ids has no row cardinality) — not applied: one row per referenced finding is the reading the row shape already forces (one `Finding:` per row); stated here rather than in the 2f-i sentence.

## Post-green polish

- **run-2 conventions/R4/F-1 / correctness/R4/F-1** — Scope row: reconciliation-boundary anchors corrected to `:85` (round-4 protocol) and `:86`, `:90-96` (console renders); `:88` no longer listed as left alone.
- **run-2 correctness/R4/F-2 / conventions/R4/F-3** — row shape: `Deferred in:` gloss names `round 4 (grill)`; `Propagation sites` parenthetical excepts a `(grill)` row; render shape's scope column is `<in-scope|out-of-scope|operator>`.
- **run-2 edge-cases/R4/F-1 (block half)** — Design 7 block: the `(grill)` exemption enumerates the two-entry floor, the at-least-one-existing-site and every-other-entry tests, and the ceiling test.
