<!-- VHS-29 fixture: the same real `log.md` header as `log-with-entries.md`
     (pre-reword, see that file's note) with its entries at `###` depth — a hand
     edit the anchor cannot describe. Drives the read-side refusal: a populated
     file whose entries the anchor misses must be a loud stop, never a silent
     bottom-append, which is this ticket's own bug wearing a green report. -->
# Wiki Log

Append-only record of wiki operations. Format: `## [YYYY-MM-DD] action | description`

> **Newest first.** `/wiki-after-merge` prepends here; if a tool appends at the
> bottom instead, the entry is out of contract — re-sort rather than leaving it.
> Older entries rotate into [`log-archive.md`](log-archive.md). This file holds
> entries dated **2026-08-01 and later**.

### [2026-08-24] merge | VHS — VHS-28: entry demoted to h3 by a hand edit

Body prose for the previously-newest entry.

### [2026-08-20] close | VHS — VHS-27: an earlier close entry (archived from TODO/)

Body prose for the older entry.
